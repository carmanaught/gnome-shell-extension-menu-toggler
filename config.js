/* 
 * Define your settings keys and specify menu labels here. The labels should match the
 * relevant <name> portion of whichever Menu.panel.statusArea.<name> menu you want to
 * use. For instance, for the Main.panel.statusArea.aggregateMenu menu we want to use
 * 'aggregateMenu'. Specify this as a label and use the labels in the extension when
 * binding shortcuts (check extension.js to compare).
 *
 * It makes the most sense to define these in pairs.
 *
 */

const aggregate_menu = 'aggregate-menu';
const aggregate_menu_name = 'aggregateMenu';
const app_menu = 'app-menu';
const app_menu_name = 'appMenu';
